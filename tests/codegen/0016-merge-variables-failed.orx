# ERROR: Not found ‘a’ in current scope:


def identity(c: bool, b: bool) -> int:
    if c:
        a: int = 123
    elif b:
        a: int = 12
    return a


def main() -> int:
    return identity(True, True)

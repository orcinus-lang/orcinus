# EXIT: 0


from tests.asserts import assert
from system.io import print


def main() -> int:
    assert((not 0) == True,  "(not 0) == True")
    assert((not 1) == False, "(not 1) == False")

    assert((0 or 2) == 2, "(0 or 2) == 2")
    assert((1 or 0) == 1, "(0 or 1) == 1")

    assert((0 and 2) == 0, "(0 and 2) == 0")
    assert((2 and 1) == 1, "(2 and 1) == 1")

    return 0

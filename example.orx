from system.io import open, print


def main() -> int:
    with open('/tmp/example.ll', 'r') as stream:
        content = stream.read(-1)

    with open('/tmp/output.ll', 'w') as stream:
        stream.write(content)

    print(content)
    return 0
